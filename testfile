concurrently
    concurrently
        over x send y
        then end
    and
        over x receive z
        then over z send z
        then end
and
    repeatedly
        restricting z in
            end
